# @webrtc-own/sdk

SDK cliente WebRTC. Sem dependências em tempo de execução. Veja `docs/protocol/sdk.md`.

## Build

```bash
cd sdk
npm install
npm run build
```

Publicação no npm acontece quando a Etapa 2 estiver funcional.
